// mock for style module (css file looking like  foo.module.css )
// this will simply return and empty object when jest encounter a css module
// file
// by default jest would try to interpret the file and faile

module.exports = {}
